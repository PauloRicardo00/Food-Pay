# Copie este arquivo para `.env` na raiz do projeto

# URL da API do backend (sem barra no final)
VITE_API_URL=http://localhost:3000/api

# true = dados fictícios (sem backend) | false = chama a API real
VITE_USE_MOCK=true

# Guia para quem integrar a API: docs/INTEGRACAO-BACKEND.md
